Drop dashboard screenshots here. Capture each at ~1280x720 (16:9), header at top, save as PNG.
Exact filenames (auto-display via onerror fallback, no code edits needed):
  cold-calling.png   leads.png   website-data.png   social.png   cloudbeds.png (optional)
